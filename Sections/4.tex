\section{Future Work}

An interesting question is what happens when we allow for arbitrarily sized simplicial complexes, both in dimension and number of simplices.
Then the class is not of transitive finite frames but arbitrary ones.
\begin{itemize}
    \item What is the geometric realization of an arbitrarily big simplicial complex?
    \item What is the ultrafilter extension of ``
    \item How do we describe such ultrafilter extension geometrically?
    \item Simplicial complexes allow for a coalgebraic description: They are a class of $\Pp\circ \Pp$ coalgebras over (finite) sets 
    (the map $V\to \Pp\Pp V$ is the one associating to every point the up-set of simplices containing it).
    Do completeness and definability results coincide to the usual ones in that setting?
\end{itemize}